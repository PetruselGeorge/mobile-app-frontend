import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const TrailCreateScreen = () => {
  return <Text style={{ fontSize: 48 }}>TrailCreateScreen</Text>;
};

const styles = StyleSheet.create({});

export default TrailCreateScreen;
